#include <iostream>
#include <sstream>
#include <string>
#include <vector>

repeatedHours = []

void int repeatedHour(){
    int n;
    string temp;
    cin >> n;
    vector<string> hours;

    for(int i = 0; i < n; i++){
        cin >> temp;
        if temp in hour:
            indexHour = hours.index(temp)
            repeatedHours[0] += 1
            if repeatedHours[0] > repeatedHours[1]:
                repeatedHours[1] = repeatedHours[0]
                repeatedHours[0] = 0
        else:
            hours.append(temp)
            repeatedHours[0] = 1 
    }
    return repeatedHour[1]
}

int main(){
    cout << repeated();    
}
